---
description: 项目铁律总览与代码输出前自检清单(always-apply)
globs:
alwaysApply: true
---

# 项目铁律总览

本规则由多个聚焦规则共同组成。生成/修改代码前,**逐条**对照下面 11 条铁律自检;违反任何一条即拒绝输出。

## 铁律速查

| #  | 铁律             | 一句话                                                      |
| -- | ---------------- | ----------------------------------------------------------- |
| 1  | 严禁滥用动态类型 | 不用 `any` / 不用 `Object` 兜底,强类型优先                   |
| 2  | 逻辑抽离与分层   | 业务逻辑与 UI 分离,跨页面共享逻辑必须抽离                    |
| 3  | 视图组件纯粹性   | UI 不发请求、不操作 DB,只接收 props / 触发回调              |
| 4  | 禁止手动改生成代码 | 生成器产物不允许手改;改上游 → 重生成                       |
| 5  | 模块与文件规范   | 新文件 ≤ 500 行、命名规范                                   |
| 6  | 零吞噬异常       | catch 必须有处理,不静默吞错                                 |
| 7  | 事务与并发安全   | 多实体写入必须事务化,敏感状态必须乐观锁                     |
| 8  | 接口响应与异步状态 | HTTP + 业务双校验,Promise 必须有 catch                     |
| 9  | 禁止硬编码 Mock  | 不在生产代码里 `return true` 假装成功                        |
| 10 | TODO 统一管理    | 代码内 TODO 必须有 `@TODO-NNN` 引用,登记在 `TODO.md`         |
| 11 | 项目特定约定     | 时区 / SQL 参数化 / 内部接口鉴权 / 上传白名单 / 第三方验签   |

## 输出前自检清单(全部 ✅ 才能输出)

- [ ] 是否引入了 `any` / 内联复杂业务类型? → 违反 #1
- [ ] 是否在 UI 组件里直接 `fetch`? → 违反 #3
- [ ] 是否跨多表写入但无事务? → 违反 #7
- [ ] 是否有 `try { } catch {}` 裸吞? → 违反 #6
- [ ] 是否有 `return true` 跳过业务逻辑? → 违反 #9
- [ ] 是否有裸 `// TODO` 注释? → 违反 #10
- [ ] 新文件是否 > 500 行? → 违反 #5
- [ ] Promise 是否都有 catch? → 违反 #8
- [ ] 内部调用是否带鉴权? → 违反 #11
- [ ] SQL 是否参数化? → 违反 #11

## 违规处理

1. **立即回滚**该段代码(包括未 commit、未推送)
2. **解释**违反的铁律与原因
3. **重新生成**合规版本
