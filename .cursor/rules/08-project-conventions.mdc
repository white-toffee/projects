---
description: 项目安全与特定约定(铁律 #11)— 时区/SQL 参数化/内部鉴权/上传/第三方验签
globs: **/*.{ts,tsx,js,jsx,sql}
alwaysApply: false
---

# 项目安全与特定约定(铁律 #11)

## 11.1 时区与时间格式

- **数据库**:所有 `*_at` / `*_time` 字段统一存 UTC ISO 8601(带 `Z` 后缀)
- **前端展示**:统一通过工具函数转换为本地时区
- ❌ 禁止视图里直接 `new Date(x).toLocaleString()` 而不做时区处理

## 11.2 SQL 安全

- 所有 SQL **必须参数化查询**
- ❌ 禁止字符串拼接 SQL
- ✅ 即使查 schema 元数据也要确保表名来自白名单

## 11.3 内部接口认证

- 内部专用端点(如带 `_internal` 命名)**必须**校验内部 token / 签名
- ❌ 禁止新增不带鉴权校验的内部端点

## 11.4 文件上传

- 限制文件大小 + 类型白名单
- 使用随机文件名存储,避免路径遍历
- ❌ 禁止保留原始文件名
- ❌ 禁止在前端直接暴露服务器内部存储路径

## 11.5 第三方签名 / 密钥

- 涉及第三方回调(支付、开放平台等)**必须验签**
- ❌ 禁止简化掉验签步骤(即使本地测试环境)
- ❌ 禁止在前端携带任何密钥 / 证书凭证

## 11.6 不提交到 git

`.gitignore` 至少包含:

```
node_modules/
dist/
.env
.env.local
.env.*.local
*.key
*.pem
```

按项目实际情况补充敏感文件、构建产物、本地数据目录等。
